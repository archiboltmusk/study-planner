{
  "installCommand": "cd deploy && npm install",
  "buildCommand": "cd deploy && npm run build",
  "outputDirectory": "deploy/dist",
  "framework": null,
  "functions": {
    "deploy/api/*.ts": {
      "runtime": "@vercel/node"
    }
  },
  "rewrites": [
    { "source": "/api/(.*)", "destination": "/deploy/api/$1" }
  ]
}
